# Introduction

Rust comes up with all essential command-line tools to develop:

- `rustup`: To install the latest version of Rust.
- `rustc`: The compiler.
- `cargo`: The package manager that can compile a project, lint, run the
  project's binaries, run tests, publish a package, ...
