# Comments

Rust uses the same comments style as C/C++: `//` and `/* ... */`.

Example with `//`:

```rust
// A C++ style comment.
```

Example with `/* ... */`:

```rust
/* A C style comment. */

/* A C style comment
   on multiple
   lines. */
```
